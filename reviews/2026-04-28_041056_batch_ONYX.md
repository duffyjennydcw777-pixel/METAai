# 🔄 Batch Review — ONYX
**Дата**: 2026-04-28 04:10
**Level**: 2
**Файлов**: 5/5
**Средний балл**: 72/100

---

## ⚠️ payments.py (65/100)

## 🔍 Code Review

### Критичные проблемы 🔴
1. Незащищенное кэширование rate limiting в памяти процесса — риск утечки памяти и проблем при горизонтальном масштабировании
2. Отсутствие проверки лимита платежей для предотвращения злоупотреблений
3. Прямое внедрение зависимостей в методе `create_invoice` (импорт time, bot)

### Предупреждения 🟡
1. Хрупкая логика резервного переключения между YooKassa и Stars
2. Потенциальная гонка условий в rate limiting механизме
3. Отсутствие четкой обработки ошибок при уведомлении администратора
4. Жесткое связывание сервисов в `activate_subscription`

### Замечания 🟢
1. Дублирование логики в методах `create_invoice` и `create_payment`
2. Сложная логика обработки реферальных бонусов
3. Отсутствие явной валидации входных данных для `plan_id`
4. Неявное использование глобальных переменных и состояний

### Вердикт
⚠️ NEEDS FIXES

### Оценка уверенности
65/100 — код функционален, но имеет существенные архитектурные недостатки и риски безопасности.

Рекомендации:
- Использовать Redis для rate limiting
- Добавить явную валидацию `plan_id`
- Разделить ответственность методов
- Улучшить обработку ошибок
- Использовать dependency injection
- Добавить юнит-тесты для сложных сценариев

---

## ⚠️ subscriptions.py (70/100)

## 🔍 Code Review

### Критичные проблемы 🔴
1. Потенциальная гонка условий (race condition) при обновлении статуса подписки. Одновременные запросы могут привести к неконсистентному состоянию базы данных.
2. Отсутствие обработки ошибок при работе с базой данных может вызвать незапланированные исключения.

### Предупреждения 🟡
1. Прямое сравнение datetime может быть неточным из-за микросекунд. Рекомендуется использовать `datetime.utcnow().replace(microsecond=0)`.
2. Нет валидации входных параметров для `service` в `check_subscription`.
3. Метод `check_subscription` дублирует логику истечения срока подписки, что нарушает принцип DRY.

### Замечания 🟢
1. Преобразование дат в ISO-формат вручную. Можно использовать `isoformat()` из стандартной библиотеки.
2. Отсутствие логирования изменений статуса подписки.
3. Жесткое кодирование статусов ("active", "expired") может усложнить поддержку.

### Вердикт
⚠️ NEEDS FIXES

### Оценка уверенности
70/100 — код работоспособен, но требует рефакторинга для повышения надежности и читаемости.

Рекомендации:
- Добавить транзакционную обработку изменения статусов
- Внедрить общую функцию проверки истечения подписки
- Реализовать более надежную обработку ошибок
- Добавить валидацию входных параметров
- Использовать константы для статусов подписок

---

## ⚠️ auth.py (75/100)

## 🔍 Code Review

### Критичные проблемы 🔴
1. Потенциальная уязвимость безопасности в `validate_init_data()`: использование статического секретного ключа `b"WebAppData"` вместо динамического ключа, производного от токена бота

### Предупреждения 🟡
1. Слишком широкое подавление исключений в блоке `try-except` — возвращается `None` без логирования ошибок
2. Отсутствие проверки на максимальную длину `init_data` может привести к атакам типа DoS
3. Небезопасное декодирование JSON без дополнительных проверок

### Замечания 🟢
1. Использовать `settings.telegram_bot_token` как резервный токен — хороший подход
2. Правильное использование `hmac.compare_digest()` для сравнения хешей
3. Проверка давности `auth_date` — корректная защита от устаревших данных
4. Аннотации типов и docstring улучшают читаемость

### Вердикт
⚠️ NEEDS FIXES

### Оценка уверенности
75/100 — код в целом безопасен, но требует незначительных улучшений безопасности и обработки ошибок.

Рекомендации:
- Использовать динамический ключ HMAC
- Добавить логирование исключений
- Ограничить максимальную длину входящих данных
- Добавить более строгую валидацию JSON

---

## ⚠️ auth.py (75/100)

## 🔍 Code Review

### Критичные проблемы 🔴
1. Потенциальная уязвимость в `get_user_from_init_data()`: нет проверки подписи Telegram, что может позволить подделать initData
2. Отсутствие обработки race condition при создании пользователя может привести к дублированию записей в многопоточной среде

### Предупреждения 🟡
1. Нет явной валидации полей пользователя перед сохранением (длина, формат)
2. Генерация `referral_code` может создавать коллизии
3. Неявное обновление данных пользователя без логирования изменений

### Замечания 🟢
1. Хорошее использование rate limiter для защиты от брутфорса
2. Корректное использование async/await и SQLAlchemy
3. Безопасная генерация referral кода через `secrets`
4. Обработка необязательных полей пользователя

### Вердикт
⚠️ NEEDS FIXES

### Оценка уверенности
75/100 

**Рекомендации:**
- Добавить проверку подписи initData
- Использовать `insert ... on conflict` для безопасного создания пользователя
- Добавить валидацию входящих данных
- Логировать изменения профиля
- Рассмотреть механизм предотвращения коллизий referral кодов

---

## ⚠️ payment_service.py (75/100)

## 🔍 Code Review

### Критичные проблемы 🔴
1. Отсутствие валидации входных параметров в `create_invoice_link()`:
   - Нет проверки корректности `amount` (может быть отрицательным)
   - Не проверяется длина `title` и `description`

### Предупреждения 🟡
1. Прямое логирование в `print()` вместо использования структурированного логирования
2. Слишком широкий `except Exception` может скрывать неожиданные ошибки
3. Жесткий таймаут 10 секунд может быть неоптимальным для разных сценариев

### Замечания 🟢
1. Хорошая практика использования именованных аргументов
2. Аннотации типов присутствуют
3. Встроенная документация информативна
4. Словари `PLAN_TITLES` и `PLAN_DESCRIPTIONS` — удобный способ хранения метаданных

### Вердикт
⚠️ NEEDS FIXES

### Оценка уверенности
75/100 — код в целом надежен, но требует доработок по безопасности и обработке ошибок.

Рекомендации:
- Добавить валидацию входных параметров
- Использовать `logging` вместо `print()`
- Рассмотреть более гибкую стратегию таймаута
- Заменить широкий `except` на специфические исключения

