# 🔄 Batch Review — Sylectus
**Дата**: 2026-04-28 03:59
**Level**: 2
**Файлов**: 1/7
**Средний балл**: 70/100

---

## ⚠️ set_admin.py (70/100)

## 🔍 Code Review

### Критичные проблемы 🔴
1. Отсутствие проверки существования пользователя перед обновлением роли. Если пользователя с указанным `telegram_id` не существует, операция пройдет без ошибки, но не обновит ни одной записи.

### Предупреждения 🟡
1. Хардкоженный `telegram_id` администратора напрямую в коде. Лучше использовать переменные окружения.
2. Нет обработки исключений при работе с базой данных. Возможны сетевые ошибки или проблемы с подключением.

### Замечания 🟢
1. Использование `sys.path.insert()` для импорта модулей вместо настройки `PYTHONPATH` или использования виртуального окружения.
2. Можно добавить логирование вместо простого `print()`.
3. Отсутствие валидации роли "admin" - нет проверки корректности значения.

### Вердикт
⚠️ NEEDS FIXES

### Оценка уверенности
70/100 — Скрипт выполнит базовую функцию, но требует улучшений безопасности и обработки ошибок.

Рекомендации:
```python
import os
from typing import Optional

async def get_user_by_telegram_id(session, telegram_id: int) -> Optional[User]:
    return await session.scalar(select(User).where(User.telegram_id == telegram_id))

async def main():
    try:
        admin_tg_id = int(os.getenv('ADMIN_TELEGRAM_ID', 923753958))
        
        async with async_session() as session:
            user = await get_user_by_telegram_id(session, admin_tg_id)
            
            if not user:
                logging.error(f"Пользователь с telegram_id={admin_tg_id} не найден")
                return
            
            user.role = "admin"
            await session.commit()
            logging.info(f"Пользователь {user.id} обновлен до роли admin")
    
    except Exception as e:
        logging.error(f"Ошибка при обновлении роли: {e}")
```

